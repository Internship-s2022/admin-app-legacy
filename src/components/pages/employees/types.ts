export interface EmployeeData {
  id: string;
  name: string;
  projects: string[];
}

export interface User {
  birthDate: Date;
  email: string;
  firstName: string;
  lastName: string;
  _id: string;
}
