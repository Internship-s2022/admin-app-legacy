export interface ClientsData {
  id?: string;
  name: string;
  projects: string;
  clientContact: string;
  email: string;
  localContact: string;
}

export interface ContactData {
  _id?: string;
  name: string;
  email: string;
}
