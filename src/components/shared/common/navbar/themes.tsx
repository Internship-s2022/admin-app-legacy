import { createTheme } from '@mui/material/styles';

export const tabTheme = createTheme({
  palette: {
    primary: {
      main: '#fff',
    },
    secondary: {
      main: '#000',
    },
  },
});
