import { createTheme } from '@mui/material/styles';

export const themes = createTheme({
  palette: {
    primary: {
      main: '#4EA381',
    },
    secondary: {
      main: '#F3F3F3',
    },
  },
});
